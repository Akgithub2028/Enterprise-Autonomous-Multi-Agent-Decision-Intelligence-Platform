# Historical Retrieval Evaluation v1

This directory retains the 50-query v1 retrieval evidence baseline. The current benchmark uses Retrieval Benchmark v2. Run the following command to verify the 200-query metrics:

```bash
python3 scripts/verify_retrieval_v2_evidence.py
```
